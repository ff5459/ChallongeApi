namespace ChallongeApi
{
    /// <summary>
    /// The <see cref="MethodType"/> is a <see langword="enum"/>, that is used to specify the http request type.
    /// </summary>
    public enum MethodType : byte
    {
        GET,
        POST,
        PUT,
        DELETE,
    }
}
